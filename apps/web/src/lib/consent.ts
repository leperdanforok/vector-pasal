/**
 * localStorage key holding the analytics consent choice: 'granted' | 'denied' | (unset).
 * Shared by the consent bar (AnalyticsGate) and the chat_submitted GA event guard.
 */
export const CONSENT_STORAGE_KEY = 'vp-analytics-consent';
